import { permanentRedirect } from "next/navigation";

/** Atajo de marca / keyword “publiescort” → landing principal. */
export default function PubliescortRedirectPage() {
  permanentRedirect("/publicaciones-escort-chile");
}
